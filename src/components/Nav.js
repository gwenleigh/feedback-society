import React from 'react';

const Nav = () => {
    return (
        <nav>
            <h2>Navigation Section</h2>
            <p>This is the navigation bar, where links to other parts of the site are placed.</p>
        </nav>
    );
};

export default Nav;
